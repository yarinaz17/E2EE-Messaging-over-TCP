# Variables that are shared between the response handling thread and main thread
# These variables are used for control and synchronization
need_to_register = True
is_connection_open = True
is_session_established = False
